# DB 변경 기록

FTP로 파일만 올리면 되는 변경인지, **`db/setup.php`를 한 번 더 열어야 하는 변경인지**를
구분하기 위한 기록입니다. 사이트 화면 변경 기록은 프로젝트 맨 위 `CHANGELOG.md`를 보세요.

## 어떻게 쓰나요

1. FTP로 파일을 올린다
2. 아래 목록에서 **내가 마지막으로 setup.php를 실행한 날짜 이후**에 `setup.php 실행` 표시가
   붙은 항목이 하나라도 있으면 → 브라우저에서 `내사이트주소/db/setup.php`를 한 번 연다
3. 없으면 → 아무것도 안 해도 됨

`setup.php`는 **몇 번을 실행해도 안전합니다.** 이미 있는 테이블·칼럼은 건너뛰고,
없는 것만 새로 만듭니다. 기존에 입력해둔 데이터는 지워지지 않습니다.

> 마지막으로 setup.php를 실행한 날짜를 여기에 적어두면 편합니다 →
> **마지막 실행: (날짜를 적어주세요)**

---

## 지금 쓰고 있는 테이블 (총 8개)

| 테이블 | 무엇이 들어있나 | 어디서 쓰나 |
|---|---|---|
| `yj_admin_users` | 로그인 계정(아이디·비밀번호·역할·실명) | 관리자 로그인 전체 |
| `yj_notices` | 공지사항 글 | 공지사항 페이지·관리 |
| `yj_license_data` | 면허 탐색기 데이터(과정·시간·수강료) 통째로 | 면허 탐색기·관리 |
| `yj_staff` | 임직원(이름·MBTI·근무형태·사진·인사말) | 홈 강사진 캐러셀·관리 |
| `yj_shuttle_riders` | 셔틀 탑승자 명단(날짜별 사람) | 셔틀 명단 관리 |
| `yj_shuttle_slots` | 셔틀 운행 편성(날짜별 차량·출발시간) | 셔틀 명단 관리 |
| `yj_student_schedule` | 학사서버에서 받아온 예약 일정 사본 | 내 일정 조회 |
| `yj_contact_messages` | 홈페이지 문의 메시지 | 문의 관리 |

`yj_`는 `config.php`의 `table_prefix` 값입니다. 설정을 바꿨다면 그 값으로 읽으세요.

### 주의: 기본 데이터 파일은 최초 1회만 반영됩니다

`db/license_default_data.json`, `notice_default_data.json`, `staff_default_data.json`은
**해당 테이블이 완전히 비어 있을 때만** DB에 들어갑니다. 이미 데이터가 있는 상태에서
이 파일들을 고쳐서 올리고 setup.php를 실행해도 **아무 일도 일어나지 않습니다.**
이미 운영 중인 내용을 고치려면 관리자 화면(면허 탐색기 관리 등)에서 수정해야 합니다.

---

## 변경 이력 (최신순)

### 2026-09-16 12:32 — `6703024` · setup.php 실행 필요
> ⚠️ 아직 `feature/shuttle-kakao-notify` 브랜치에만 있습니다. main에는 없으므로
> 지금 FTP로 올리는 파일에는 포함되지 않습니다. 이 기능을 실제로 쓸 때 실행하세요.

셔틀 카카오 알림톡 발송 준비. `yj_shuttle_riders`에 칼럼 3개 추가:
- `notified_at` — 알림톡을 보낸 시각 (안 보냈으면 비어 있음)
- `notify_status` — 발송 상태
- `notify_error` — 실패했을 때 이유

명단을 저장하면 그 날짜 명단을 통째로 지우고 다시 넣는 구조라, 이름·연락처·탑승시간·
장소가 전부 그대로인 사람만 발송 상태를 이어받고, 뭔가 바뀐 사람은 "미발송"으로 돌아갑니다.

### 2026-09-14 19:18 — `82013c9` · setup.php 실행 필요
강사·사무실·셔틀 계정 분리(겸직 가능). 칼럼 추가:
- `yj_admin_users.role` — 역할을 쉼표로 여러 개 저장 (`admin`/`manager`/`office`/`instructor`)
- `yj_admin_users.real_name`, `yj_staff.real_name` — 학사서버 예약을 본인 것만 걸러보기 위한
  비공개 실명 (홈페이지에 공개되는 이름과 별개)
- `yj_admin_users.staff_id` — 임직원 카드에서 자동 생성된 계정 연결용

### 2026-09-13 14:28 — `9f23cf1` · setup.php 실행 필요
내 일정 조회에 면허 종류 표시. `yj_student_schedule.license_type` 칼럼 추가
(같은 사람이 면허를 여러 개 등록한 경우 구분용).

### 2026-09-12 21:07 — `36f5048` · setup.php 실행 필요
수강생 본인 일정 조회 기능. `yj_student_schedule` 테이블 신규 생성.
학사서버(MSSQL)에서 주기적으로 밀어넣는 사본이라 동기화할 때마다 전체를 지우고 다시 채웁니다.
주민번호 같은 민감정보는 애초에 안 들어갑니다.

### 2026-09-12 14:01 — `b1eb47a` · setup.php 실행 필요
홈페이지 문의하기 기능. `yj_contact_messages` 테이블 신규 생성 (읽음/안읽음 상태 포함).

### 2026-09-10 00:08 — `245a3ed` · setup.php 실행 필요
셔틀 차량 지정·사람별 탑승시간. 칼럼 추가:
- `yj_shuttle_riders.slot_no`, `yj_shuttle_slots.slot_no` — 시간이 바뀌어도 소속이 유지되도록
  시간이 아니라 "편성 번호"로 묶음
- `yj_shuttle_riders.board_time` — 장소마다 태우는 시각이 달라서 사람별 탑승시간을 따로 저장
- `yj_shuttle_slots.vehicle` — 차량 호수/차량번호

옛날 데이터에 편성 번호를 자동으로 매기는 작업도 같이 들어있습니다 (여러 번 실행해도 안전).

### 2026-09-09 20:53 — `0d6e207` · setup.php 실행 필요
탑승자가 없는 시간대도 저장되도록 `yj_shuttle_slots` 테이블 신규 생성.

### 2026-09-09 20:36 — `028f39a` · setup.php 실행 필요
셔틀 명단만 볼 수 있는 office 계정 분리. `yj_admin_users.role` 칼럼 최초 추가.

### 2026-09-09 16:36 — `76af0c9` · setup.php 실행 필요
셔틀 명단 기능 시작. `yj_shuttle_riders` 테이블 신규 생성
(+ `updated_by` — 누가 마지막으로 고쳤는지).

### 2026-09-07 21:34 — `311f6b8` · setup.php 실행 필요
강사진 인사말. `yj_staff.greeting` 칼럼 추가.

### 2026-09-07 16:40 — `a4883d9` · setup.php 실행 필요
임직원 관리 기능 시작. `yj_staff` 테이블 신규 생성.

---

### 파일만 바뀐 변경 (setup.php 실행 불필요)

아래는 `db/` 폴더를 건드렸지만 **기본 데이터 JSON만** 고친 것들입니다. 위에 적은 대로
이 파일들은 테이블이 비어 있을 때만 반영되므로, 이미 운영 중인 DB에는 영향이 없습니다.
실제 수강료·시간 수정은 전부 관리자 화면에서 이루어졌습니다.

- `0ec5221` (2026-09-14) — 2종소형 학과교육 시간 오류
- `3e9be18` (2026-09-09) — TM-01~03 도로주행비, B1-06 기준, 구난차 과정 삭제
- `f6173cc` (2026-09-09) — TM-04·CD-01 교육과정, CD 요율
- `c2db1a2` (2026-09-08) — 0시간/비용 불일치 오류

---

## 기록 규칙

앞으로 DB 구조(테이블·칼럼)를 건드리는 커밋을 할 때마다 이 파일 맨 위에 항목을 추가합니다.
적는 내용은 **날짜·커밋해시·setup.php 실행이 필요한지·무엇이 왜 바뀌었는지** 네 가지입니다.
