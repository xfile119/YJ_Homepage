# 변경 기록

FTP로 실제 사이트에 반영하기 전에, 무엇이 바뀌었는지 쉬운 말로 확인하는 용도입니다.
정확한 코드 변경 내용은 커밋 해시로 `git show <해시>`를 보면 됩니다.

사이트에 접속하면 화면 맨 아래(푸터) 작은 글씨로 `build 날짜시각 · 커밋해시`가 표시됩니다.
FTP 업로드 후 이 값이 아래 최신 항목과 일치하는지 보면, 실제로 반영됐는지 바로 확인할 수 있습니다.

**DB(테이블·칼럼) 변경은 별도 파일로 관리합니다 → [`db/CHANGELOG-DB.md`](db/CHANGELOG-DB.md)**
파일만 올리면 되는지, `db/setup.php`를 한 번 더 실행해야 하는지가 거기 적혀 있습니다.

---

## 2026-09-20 07:10 KST — `e513074`
필기시험 저장 API(`api/written-exam.php`) 신설 — **화면상 눈에 보이는 변화는
없습니다.** 사무실에서 쓰는 안내장 앱(guide-print)이 상담 후 정한 필기시험
날짜를 이 API를 통해 저장/조회/삭제합니다. 셔틀과 같은 방식(홈페이지 DB에
저장)이고, 학사DB(neoinfo)는 전혀 건드리지 않습니다.

**FTP로 `api/written-exam.php`, `api/_db.php`를 올리고, `db/setup.php`를
한 번 더 실행해주세요** (새 테이블 `yj_written_exam` 생성). 자세한 내용은
[`db/CHANGELOG-DB.md`](db/CHANGELOG-DB.md) 참고.

또한 `config.php`에 `written_exam_sync_key`를 새로 추가해야 합니다
(`config.example.php` 참고) — 안내장 앱 쪽 `config.json`에도 같은 값을
넣어야 서로 인증됩니다.

## 2026-09-17 15:20 KST — `0c4850d` 이후
공유 이미지가 없던 4개 페이지에 추가 — 셔틀버스 조회, 내 일정 조회,
신체검사 병원 안내, 셔틀/일정 조회 앱. 이제 이 주소들을 카톡으로 보내도
글씨만 나오지 않고 배너가 함께 뜹니다.

지금은 대표 배너(`og-cover-2026.jpg`)를 같이 씁니다. 페이지별 전용 배너가
준비되면 **각 HTML의 `og:image` 줄에서 파일명만 바꾸면** 됩니다. 어디를 고칠지
바로 보이도록 그 줄 위에 안내 주석을 달아뒀습니다.

| 페이지 | 넣을 파일명 (권장) |
|---|---|
| 셔틀버스 조회 | `images/og-shuttle.jpg` |
| 내 일정 조회 | `images/og-my-schedule.jpg` |
| 신체검사 병원 안내 | `images/og-hospitals.jpg` |
| 셔틀/일정 조회 앱 | `images/og-lookup-app.jpg` |

배너는 모두 **1200×630** 크기로 만들어 `images/` 폴더에 넣으면 됩니다.

## 2026-09-17 15:05 KST — `4bf0c1d`
카카오톡 공유 이미지를 새 배너(아이 캐릭터 + 도로 배경)로 교체.
**FTP로 `images/og-cover-2026.jpg`를 꼭 같이 올려야 합니다.**

파일명을 바꾼 이유: 카카오는 링크 미리보기를 한 번 만들면 저장해두고 원본을 다시
읽지 않습니다. 9/16에 보라색으로 바꾸고 서버에도 정상 반영됐는데 카톡에는 계속
9/9의 주황색이 나오고 있었습니다. 이름이 바뀌면 카카오가 모르는 주소라 새로 읽어갑니다.

## 2026-09-17 13:40 KST — `9d0fefe`
DB 변경 기록(`db/CHANGELOG-DB.md`) 신설. 지금까지의 테이블·칼럼 변경 이력을 모아서
정리하고, 각 항목마다 `setup.php` 재실행이 필요한지 표시. 사이트 화면은 바뀐 것 없음
(문서만 추가되어 푸터 버전 표시는 그대로 둠).

## 2026-09-17 12:15 KST — `815eb3e`
헤더·푸터의 보라색 원형 로고(인라인 SVG 스티어링휠 아이콘)를 마스코트 이미지로
교체 — 지금까지 파비콘·og-image만 바꾸고 정작 이 로고는 안 바뀐 채로 남아있었음.

## 2026-09-17 10:51 KST — `e655688`
이미지로 면허 찾기(picker)를 기본 면허 탐색기로 전환. 메인메뉴·푸터의 "면허 탐색기"가
이제 이미지로 고르는 버전으로 연결되고, 텍스트 버전은 보조 화면이 됨. 위저드 첫 화면의
"텍스트로 찾기/이미지로 찾기" 전환 링크를 화면 상단으로 이동.

## 2026-09-16 22:02 KST — `2084ac2`
면허 탐색기 위젯의 JetBrains Mono 글씨 굵기(400) 누락 수정. 사이트 전체 공개 페이지에
"본문 바로가기" 스킵링크 추가(키보드 접근성).

## 2026-09-16 21:56 KST — `a6af7c0`
면허 탐색기 위젯에서 지정만 해두고 실제로 안 불러오던 폰트(Gothic A1, JetBrains Mono)
연결.

## 2026-09-16 21:46 KST — `1b4b4a8`, `25652fe`
Claude Code 스킬 설치: `redesign-existing-projects`(디자인 개선 점검), `web-design-guidelines`
(접근성·UX 점검). React/Tailwind 전제라 이 사이트에 안 맞던 `design-taste-frontend`는
설치 후 제거.

## 2026-09-16 21:37 KST — `2f37e60`
Claude Code 스킬 설치: `web-design-guidelines`.

## 2026-09-16 20:59 KST — `33259d1`
파비콘(브라우저 탭 아이콘)과 카카오톡 공유 미리보기 이미지를 셔틀 마스코트 캐릭터로 교체.

## 2026-09-16 20:55 KST — `855b8c3`
사이트 전체 22개 페이지에 파비콘 연결(이전엔 아예 연결이 안 되어 크롬 탭에 지구본
아이콘이 뜨고 있었음).

---

*(2026-09-17 이전 기록은 git 커밋 로그 참고: `git log --oneline`)*
