# 변경 기록

FTP로 실제 사이트에 반영하기 전에, 무엇이 바뀌었는지 쉬운 말로 확인하는 용도입니다.
정확한 코드 변경 내용은 커밋 해시로 `git show <해시>`를 보면 됩니다.

사이트에 접속하면 화면 맨 아래(푸터) 작은 글씨로 `build 날짜시각 · 커밋해시`가 표시됩니다.
FTP 업로드 후 이 값이 아래 최신 항목과 일치하는지 보면, 실제로 반영됐는지 바로 확인할 수 있습니다.

**DB(테이블·칼럼) 변경은 별도 파일로 관리합니다 → [`db/CHANGELOG-DB.md`](db/CHANGELOG-DB.md)**
파일만 올리면 되는지, `db/setup.php`를 한 번 더 실행해야 하는지가 거기 적혀 있습니다.

---

## 2026-09-26 15:00 KST — `804ffb9`
**오시는 길: 셔틀 무료 안내 명시.** 셔틀 운영 방식을 사장님께 확인 — 무료 운영,
정기 노선 없이 신청하신 분 위주로 픽업하는 방식. 기존 문구는 무료 여부를 밝히지
않아 방문자가 비용이 있는지 헷갈릴 수 있었던 점을 보완했습니다.

**FTP로 올릴 파일**: `location.html`, `js/main.js`. DB 변경 없음.

---

## 2026-09-26 02:20 KST — `f0b79ca`
**사용성 체크리스트 반영.** 외부에서 받은 정보 전달력·동선 평가 문서의 "개선 필요" 항목을
코드로 재확인한 뒤, 실제 문제인 것만 고쳤습니다.

- **면허 탐색기**: 취득 과정에 "필기시험" 단계 추가(학과교육만 들으면 끝나는 걸로 오해할 수
  있었음). 예상 합계 각주를 "필기시험 응시료·신체검사 비용·면허증 발급 수수료는 포함 안 됨"으로
  명확히. "교육 가능 시간"→"학과교육 시간표", "시험 일정"→"시험 일정 확인 방법"(+내 일정 조회
  링크)로 제목과 내용을 맞춤. 오탈자 "최短"→"최단". 수강료 API 실패 시 "참고용 기본값" 안내 추가.
- **학과교육 시간표 페이지**: 방문자에게 의미 없는 내부 작업 문구 삭제, 실제 안내로 교체.
- **작은 안내문 글자색**: 흰 배경 대비가 부족했던 회색(#7A8699→#64748B)을 사이트 전체에서 교체.
- **모바일 상담 버튼**: 전화·카카오·네이버가 항상 펼쳐져 있어 본문과 겹치던 문제 — "상담" 버튼
  하나로 접어두고 누르면 펼쳐지도록 변경.
- **모바일 메뉴**: Esc로 닫기, 열림 상태에 따라 "메뉴 열기/닫기" 안내 갱신 추가.

**FTP로 올릴 파일**: `css/style.css`, `education-hours.html`, `js/license-wizard.js`,
`js/main.js`. DB 변경 없음.

## 2026-09-26 01:35 KST — `1e96fce`
**홈 카드 곡률 16px + 그라데이션 제거.** 지난 리뷰 때 결정이 필요했던 두 가지를
사용자 결정대로 반영했습니다.

- 홈 진입 카드(entry-card) 모서리를 22px → 16px로.
- 히어로, 페이지 상단 배너, CTA 배너, 대형연수 홍보 배너의 남색 그라데이션을
  단색(`--color-deep`, 헤더·푸터와 같은 색)으로 교체. 그라데이션보다 단색이
  대비가 또렷해 눈에 더 잘 들어온다는 판단입니다.

**FTP로 올릴 파일**: `css/style.css`. DB 변경 없음.

## 2026-09-26 01:10 KST — `89722db`
**버그 수정 3건 + 홈 카드 디자인 다듬기.** 외부 리뷰에서 나온 지적을 하나씩 재현해서
확인한 뒤, 실제 문제인 것만 고쳤습니다.

- **[높음] 통합조회 탭 이동 중 요청 취소 안 됨**: 조회 중 다른 탭으로 이동했다가 돌아오면
  빈 칸 아래에 이전 조회 결과가 다시 나타날 수 있던 문제. 탭을 떠나면 진행 중이던 요청을
  취소하도록 고쳤습니다.
- **[높음] `db/check.php` 인증 없이 서버 정보 노출**: PHP 버전·DB 연결 상태 등을 누구나 볼 수
  있었습니다. `db/setup.php`와 같은 방식으로, 최고관리자 계정이 있으면 로그인한 최고관리자만
  볼 수 있게 막았습니다.
- **[중간] 서비스워커가 다른 앱 캐시까지 삭제**: 면허 탐색기(license-picker) 앱을 업데이트하면
  면허가이드·조회 앱의 오프라인 캐시까지 같이 지워지던 문제. 각자 자기 캐시만 정리하도록
  고쳤습니다.
- 홈 화면 카드: 오른쪽 "조회" 버튼이 왼쪽보다 위에 떠 있던 정렬 문제, 왼쪽 카드 작은
  안내문의 낮은 글자 대비, 통합조회 탭의 방향키(←/→) 이동을 추가로 다듬었습니다.

**FTP로 올릴 파일**: `css/style.css`, `db/check.php`, `my-schedule.html`,
`sw-license-guide.js`, `sw-license-picker.js`, `sw-lookup-app.js`. DB 변경 없음.

**올린 직후 확인**: `db/check.php`를 로그아웃 상태에서 열어봤을 때 "접근할 수 없습니다"가
뜨는지 확인해주세요. (로그인한 최고관리자로는 그대로 열립니다.)

## 2026-09-26 00:20 KST — `4afc8b4` (브랜치: claude/fervent-pasteur-m0azj3)
**홈 진입 카드 디자인 최종안.** 캔버스 목업에서 다듬은 디자인을 실제 `index.html`에 반영.

- 카드 전체가 링크입니다. 노란 버튼뿐 아니라 카드 어디를 눌러도 이동합니다.
- 마우스를 올리면 진한 남색으로 카드가 바뀝니다 (좌측 탐색기는 흰 배경→남색, 우측
  내 일정 조회는 남색→더 진한 남색).
- 문구를 질문형으로 교체: "나에게 맞는 면허, 어디서부터 시작할까요?" / "내 일정
  확인 하기". 교육·셔틀·필기 칩 3개는 빼고 한 줄 설명으로 정리.
- 카드 안 요소를 전부 가운데 정렬. 키보드로 넘어갈 때도 같은 강조가 뜨도록
  포커스 표시 추가.

**FTP로 올릴 파일**: `index.html`, `css/style.css`. DB 변경 없음.

## 2026-09-25 23:50 KST — `e514847` (브랜치: claude/fervent-pasteur-m0azj3)
**홈 진입 동선 개편 + 내 일정 조회 3종 통합 + 사이트 색상 전환.** 캔버스 목업으로
먼저 검토한 세 가지를 새 브랜치에 반영했습니다. **main이 아닌 별도 브랜치입니다 —
검토 후 main에 반영할지 결정해주세요.**

- **홈 진입 배너**: 히어로 아래·기아차 제휴 띠 위에 신규/재원 2단 카드 추가.
  좌측(탐색기)은 "면허, 선택만 하세요. 취득 과정·비용·기간, 나머지는 저희가
  찾아드릴게요.", 우측은 "내 일정 조회 (교육·셔틀·필기)". 히어로의 "면허 탐색기
  보기" 버튼은 중복이라 제거.
- **내 일정 조회 통합**: `my-schedule.html`에 교육/셔틀/필기 탭 3개 추가.
  탭을 바꾸면 떠나는 탭의 입력값·조회 결과가 비워져 매번 새로 조회합니다
  (API는 기존 3개 그대로 재사용). 모든 페이지 메뉴에서 "셔틀 조회"·"필기시험
  조회" 링크를 삭제하고 "내 일정 조회" 하나만 남겼습니다. `shuttle.html`·
  `written-exam.html` 페이지 자체는 인쇄물 등 기존 링크를 위해 당분간 남겨둡니다.
- **색상 전환**: 보라(#6D28D9) → 네이비(#13294B) + 옐로우(#F5B700, 기존 강조색
  그대로). 도로 표지판(경고=노랑·안내=남색) 연상과 신뢰감을 노렸습니다. `css/style.css`의
  `:root` 색상 토큰과, 하드코딩돼 있던 히어로·페이지 상단 배너·CTA 배너의 보라
  그라데이션 4곳, PWA `theme-color`·manifest 5곳을 함께 고쳤습니다.

**FTP로 올릴 파일**: `css/style.css`, `index.html`, `my-schedule.html`,
`about.html`, `contact.html`, `driving-lessons.html`, `education-hours.html`,
`large-vehicle-training.html`, `license-guide.html`, `license-picker.html`,
`location.html`, `lookup-app.html`, `manifest-license-guide.json`,
`manifest-license-picker.json`, `manifest-lookup-app.json`,
`medical-checkup-hospitals.html`, `notice-detail.html`, `notice.html`,
`privacy.html`, `shuttle.html`, `written-exam.html`. DB 변경 없음.

**올린 직후 확인**: 홈 화면의 새 2단 배너, `my-schedule.html`의 탭 3개(로그인
없이 바로 조회 가능), 전체 화면 톤이 네이비+옐로우로 바뀌었는지 확인해주세요.

## 2026-09-25 23:20 KST — `7b46bb6`
**공지사항 예시 데이터 삭제 + 셔틀버스 안내 문구 수정.**

- 실제 사이트는 이미 공지사항을 모두 지운 상태라, 저장소에 남아있던 예시 공지
  (학과교육 시간표 변경, 여름 성수기 셔틀버스 임시 노선 등)를 지웠습니다.
  홈·공지사항 페이지 모두 공지가 없으면 "등록된 공지사항이 없습니다"로 보입니다.
  `db/setup.php`를 다시 실행해도 이 예시 공지가 되살아나지 않도록 시드 파일도 비웠습니다.
- **셔틀버스** 안내 문구를 "신청한 수강생만 운행합니다"에서 "신청해주신 수강생분들께는
  모두 셔틀을 운행해 드립니다. 다만 정기 노선으로 운행하지는 않아 미리 신청을
  부탁드리고 있으며, 탑승 시간과 장소는 조회를 통해 확인하실 수 있습니다."로 수정.

**FTP로 올릴 파일**: `index.html`, `notice.html`, `location.html`, `db/notice_default_data.json`.
DB 변경 없음(실제 사이트는 이미 비어 있음).

## 2026-09-25 23:05 KST — `d2c3de4`
**오시는 길 정리.** 예시로 들어가 있던 셔틀 노선표(시청·지하철역·신도시 방면)와
지하철·버스 안내의 "[…입력해주세요]" 임시 문구를 지웠습니다.

- **셔틀버스**: 정해진 노선 없이 신청한 수강생만 운행하며, 등록할 때 신청하면 되고
  탑승 시간은 셔틀 조회에서 확인할 수 있다고 안내. 카드를 누르면 셔틀 조회로 이동.
- **자가용**: 카드를 누르면 네이버 지도에서 '영진자동차운전전문학원' 검색 결과가 새 창으로 열림.
- 페이지 상단 소개 문구와 검색 결과 설명(대중교통·노선 표현)도 맞춰 고침.

**FTP로 올릴 파일**: `location.html`, `js/main.js`. DB 변경 없음.

## 2026-09-24 00:58 KST — `f45a968`·`83ba595`
- **오시는 길**: 팩스 칸에 "[팩스번호를 입력해주세요]"가 그대로 보이던 것을 062-951-5107로 교체.
  ⚠️ 같은 페이지의 **셔틀 노선·시간표**("시청·지하철역·신도시 방면" 예시와 "실제 노선/시간표로 교체 필요" 표시),
  **지하철·버스 안내**("[…입력해주세요]")가 아직 임시 문구로 보이고 있습니다. 실제 정보를 주시면 바로 바꿉니다.
- **QR 만들기** 종류에 "카카오톡 채널 메시지" 추가.

**FTP로 올릴 파일**: `location.html`, `admin-stats.html`, `js/main.js`. DB 변경 없음.

## 2026-09-24 00:53 KST — `defebb5`
관리자 **"광고·유입 성과"에 QR 만들기** 추가. 전단·현수막·명함·블로그·인스타 등 종류와
이름(예: 전단을 돌린 아파트)을 정하면, 그 이름으로 따로 집계되는 주소와 인쇄용 QR 이미지를
만들어줍니다. 어느 단지 전단이 상담으로 이어졌는지 "광고 키워드·캠페인별" 표에서 비교할 수 있습니다.

**FTP로 올릴 파일**: `admin-stats.html`, `js/main.js`, `js/yj-channels.js`,
`js/vendor/qrcode-generator-1.4.4.js`(새 폴더·새 파일). DB 변경 없음.

## 2026-09-24 00:40 KST — `95ab296`
**광고·유입 성과 측정 추가.** 광고를 켜기 전에, 어디서 들어온 방문자가 상담 신청·문의·
전화·카톡으로 이어졌는지 셀 수 있게 했습니다. 관리자 메인의 **"광고·유입 성과"**에서
기간별로 보고, 광고비를 넣으면 상담 1건당 비용이 나옵니다. 문의 관리의 각 문의에도
"유입: 네이버 파워링크 · 광고 키워드 …"처럼 어디서 온 사람인지 표시됩니다.

- 광고·업체 정보의 홈페이지 주소 칸에는 성과 화면의 **"광고에 넣을 주소"**를 그대로 쓰세요.
- 이름·전화번호 같은 개인정보는 통계에 저장하지 않습니다(경로·키워드·페이지만, 13개월 보관).
- 전화·카톡 수는 "버튼을 누른 횟수"라 실제 통화 여부와는 다릅니다. 네이버 플레이스 안에서
  바로 건 전화는 홈페이지를 거치지 않아 잡히지 않습니다.

**FTP로 올릴 파일**: `js/main.js`, `js/yj-channels.js`(새 파일), `js/license-wizard.js`,
`api/track.php`(새 파일), `api/_db.php`, `api/contact.php`, `admin-stats.html`(새 파일),
`admin.html`, `admin-messages.html`, `contact.html`, `sw-license-guide.js`, `sw-license-picker.js`.
DB 테이블·칼럼이 추가되지만 처음 기록할 때 자동으로 만들어집니다.

## 2026-09-24 00:24 KST — `e410639`
홈(`index.html`)에 **네이버 서치어드바이저 소유 확인 태그** 추가. 홈페이지가 네이버 검색에서
어떤 검색어로 몇 번 노출·클릭되는지 볼 수 있게 됩니다.

**FTP로 올릴 파일**: `index.html`, `js/main.js`. 올린 뒤 서치어드바이저에서 "소유 확인"을 누르고,
**요청 → 사이트맵 제출**에 `https://www.yjcdrive.co.kr/sitemap.xml`을 넣으세요.

## 2026-09-24 00:18 KST — `2ce09e0`
**검색 설정 정리.** 외부 SEO 분석에서 지적된 설정 충돌을 실제 파일과 대조해 고쳤습니다.

- `sitemap.xml`: 검색 제외 페이지(`license-guide.html`) 대신 주 메뉴의 `license-picker.html`,
  빠져 있던 `medical-checkup-hospitals.html` 추가. 수정일을 실제 파일 수정일로 바꿈.
- `robots.txt`: 셔틀 조회·공지 상세 차단 해제(페이지 안의 검색 제외 표시가 제대로 읽히도록).
- `.htaccess`: `yjcdrive.co.kr`(www 없음)으로 들어오면 `https://www.yjcdrive.co.kr`로 이동.

**FTP로 올릴 파일**: `sitemap.xml`, `robots.txt`, `.htaccess`, `js/main.js`. DB 변경 없음.
**올린 직후 꼭 확인**: 주소창에 `yjcdrive.co.kr`과 `http://www.yjcdrive.co.kr`을 각각 쳐서
`https://www.yjcdrive.co.kr`로 바뀌는지, 사이트가 정상으로 뜨는지 보세요. 만약 화면이 안 뜨면
(500 오류 등) 예전 `.htaccess`로 바로 되돌리면 됩니다.

## 2026-09-23 21:26 KST — `57460a4`
**개인정보처리방침을 실제와 맞게 고치고, 1개월 지난 문의를 자동 삭제합니다.**
처리방침에 "홈페이지로는 개인정보를 수집하지 않는다"고 되어 있었는데, 문의 남기기와
면허 탐색기 상담 신청은 이름·연락처를 받고 있어 사실과 달랐습니다.

- 처리방침(`privacy.html`): 처리목적(제1조)·보유기간 1개월(제2조)·수집 항목(제6조,
  필수 이름·연락처 / 선택 문의 내용·연락 가능한 시간·상담 원하는 과정)·변경 이력(제14조) 추가
- 문의는 **접수일로부터 1개월이 지나면 자동으로 삭제**됩니다. 계속 관리가 필요한 분은
  그 전에 학사 시스템 등으로 옮겨주세요(문의 관리 화면에도 안내가 있습니다).
- 문의 남기기·상담 신청 양식의 동의 문구에 수집 항목·목적·보유기간을 적었습니다.

**FTP로 올릴 파일**: `privacy.html`, `contact.html`, `admin-messages.html`,
`api/contact.php`, `js/license-wizard.js`, `js/main.js`. DB 변경 없음.
처리방침의 변경일(2026. 9. 23.)은 실제로 올리는 날과 다르면 `privacy.html` 제14조에서 고쳐주세요.

## 2026-09-23 20:59 KST — `0e94a5d`
**면허 탐색기 결과에 "이 과정 상담 신청하기" 추가.** 예상 합계 바로 아래 버튼을
누르면 그 자리에서 상담 신청 양식이 펼쳐집니다. 선택한 과정·코드·선택 경로·
예상 합계가 자동으로 함께 전달되어, 상담원이 전화하기 전에 무슨 과정 문의인지
바로 알 수 있습니다(텍스트로 찾기·이미지로 찾기 두 화면 모두).

- **연락 가능한 시간**은 플립시계로 하나만 고릅니다(09:00~18:00, 30분 단위).
  "최대한 원하시는 시간에 연락드리겠지만, 약간의 오차가 발생할 수 있습니다." 안내 포함.
- 관리자 **문의 관리**에 "면허 탐색기 상담 신청" 표시와 함께 연락 가능 시간·과정·
  금액이 따로 보이고, **관리자 메인**의 문의 관리 카드에 안 읽은 문의가 있으면
  빨간 **"새 문의 N"** 뱃지가 붙습니다.
- 신청은 한 브라우저에서 10분에 5번까지만 받습니다(중복·장난 제출 방지).

**FTP로 올릴 파일**: `js/license-wizard.js`, `api/contact.php`, `admin.html`,
`admin-messages.html`, `sw-license-guide.js`, `sw-license-picker.js`, `js/main.js`.
DB 칼럼이 추가되지만 처음 쓸 때 자동으로 만들어집니다 — 자세한 건
`db/CHANGELOG-DB.md`.

## 2026-09-23 19:31~20:52 KST — 보안 점검 반영 (`ecf26e3`~`0de0fc2`)
외부 보안 검토에서 나온 긴급·높음 항목을 고쳤습니다.

- **설치 화면 잠금** (`ecf26e3`, `f8c31ed`): `db/setup.php`가 서버에 남아 있어도,
  최고관리자 계정이 있으면 최고관리자로 로그인한 상태에서만 열립니다. office1~3
  공용 계정은 더 이상 만들지 않습니다(이미 있는 건 "계정 관리"에서 삭제).
- **`db/` 안의 스키마·기본데이터 파일 직접 열람 차단** (`fb46fab`, 새 파일 `db/.htaccess`).
- **계정 삭제·비밀번호·역할 변경 즉시 반영** (`b57a179`): 바꾸는 순간 그 계정의 예전
  로그인은 바로 끊깁니다. 로그인할 때마다 세션ID도 새로 발급합니다.
- **면허 탐색기 화면 스크립트 삽입 차단** (`cb03acb`): 관리자 화면에서 입력한 문구가
  방문자 화면에서 코드로 실행되지 않고 글자 그대로만 보입니다.
- **관리자 로그인 시도 횟수 제한** (`2fa3e09`): 같은 IP에서 15분 안에 10번 틀리면 잠시 막힘.
- **다른 사이트를 통한 몰래 요청(CSRF) 차단** (`b57dfb9`): 관리자 저장·삭제 요청은
  우리 사이트 화면에서 보낸 것만 받습니다.
- **배포 순서로 로그인이 막히지 않게** (`0de0fc2`): 새 DB 칼럼·테이블이 없으면 로그인할
  때 자동으로 만듭니다.

**올린 직후 한 번은 다시 로그인해야 합니다**(로그인 정보 저장 방식이 바뀌어서
기존 로그인은 자동으로 풀립니다).

**FTP로 올릴 파일**: `api/_db.php`, `api/auth.php`, `api/accounts.php`, `api/staff.php`,
`api/license-data.php`, `js/license-wizard.js`, `admin-accounts.html`, `db/.htaccess`(새 파일).
`db/setup.php`는 서버에서 지워두셨다면 굳이 다시 올릴 필요는 없습니다(필요한 DB 변경은
자동으로 됩니다). 올려서 실행하려면 먼저 관리자로 로그인한 뒤 여세요.

## 2026-09-20 09:05 KST — `e6e2cf5`
셔틀 명단(`admin-shuttle.html`) 인쇄할 때 연락처 칸이 좁아 전화번호가 잘려
보이던 문제 수정. 다들 010으로 시작하니 인쇄물에서만 앞자리를 빼고 짧게
보여줍니다(예: `1234-5678`). 화면에서 입력·저장하는 실제 번호는 그대로
010부터 다 남아있고, 인쇄할 때 잠깐만 짧게 바뀌었다가 원래대로 돌아옵니다.

**FTP로 `admin-shuttle.html`만 올리면 됩니다.** DB 변경 없음.

## 2026-09-20 08:20 KST — `3f8ab0f`
**필기시험 조회 페이지(`written-exam.html`) 신설** — 이름+전화번호 뒷 4자리로
등록된 필기시험 날짜를 확인할 수 있습니다. 셔틀 조회와 같은 자리(상단바·
푸터)에 나란히 추가했습니다. 아직 조회만 되고, 신청·변경·취소는 없습니다
(사무실에서 안내장 앱으로 등록해드립니다).

**FTP로 올릴 파일**: `written-exam.html`(새 파일), `api/written-exam.php`,
그리고 링크가 추가된 15개 페이지(`about.html`, `contact.html`,
`driving-lessons.html`, `education-hours.html`, `index.html`,
`large-vehicle-training.html`, `license-guide.html`, `license-picker.html`,
`location.html`, `medical-checkup-hospitals.html`, `my-schedule.html`,
`notice-detail.html`, `notice.html`, `privacy.html`, `shuttle.html`).

**`db/setup.php`를 한 번 더 실행해주세요** — `yj_written_exam` 테이블에
`student_phone` 컬럼이 추가됩니다. 자세한 내용은
[`db/CHANGELOG-DB.md`](db/CHANGELOG-DB.md) 참고.

## 2026-09-20 07:10 KST — `e513074`
필기시험 저장 API(`api/written-exam.php`) 신설 — **화면상 눈에 보이는 변화는
없습니다.** 사무실에서 쓰는 안내장 앱(guide-print)이 상담 후 정한 필기시험
날짜를 이 API를 통해 저장/조회/삭제합니다. 셔틀과 같은 방식(홈페이지 DB에
저장)이고, 학사DB(neoinfo)는 전혀 건드리지 않습니다.

**FTP로 `api/written-exam.php`, `api/_db.php`를 올리고, `db/setup.php`를
한 번 더 실행해주세요** (새 테이블 `yj_written_exam` 생성). 자세한 내용은
[`db/CHANGELOG-DB.md`](db/CHANGELOG-DB.md) 참고.

또한 `config.php`에 `written_exam_sync_key`를 새로 추가해야 합니다
(`config.example.php` 참고) — 안내장 앱 쪽 `config.json`에도 같은 값을
넣어야 서로 인증됩니다.

## 2026-09-17 15:20 KST — `0c4850d` 이후
공유 이미지가 없던 4개 페이지에 추가 — 셔틀버스 조회, 내 일정 조회,
신체검사 병원 안내, 셔틀/일정 조회 앱. 이제 이 주소들을 카톡으로 보내도
글씨만 나오지 않고 배너가 함께 뜹니다.

지금은 대표 배너(`og-cover-2026.jpg`)를 같이 씁니다. 페이지별 전용 배너가
준비되면 **각 HTML의 `og:image` 줄에서 파일명만 바꾸면** 됩니다. 어디를 고칠지
바로 보이도록 그 줄 위에 안내 주석을 달아뒀습니다.

| 페이지 | 넣을 파일명 (권장) |
|---|---|
| 셔틀버스 조회 | `images/og-shuttle.jpg` |
| 내 일정 조회 | `images/og-my-schedule.jpg` |
| 신체검사 병원 안내 | `images/og-hospitals.jpg` |
| 셔틀/일정 조회 앱 | `images/og-lookup-app.jpg` |

배너는 모두 **1200×630** 크기로 만들어 `images/` 폴더에 넣으면 됩니다.

## 2026-09-17 15:05 KST — `4bf0c1d`
카카오톡 공유 이미지를 새 배너(아이 캐릭터 + 도로 배경)로 교체.
**FTP로 `images/og-cover-2026.jpg`를 꼭 같이 올려야 합니다.**

파일명을 바꾼 이유: 카카오는 링크 미리보기를 한 번 만들면 저장해두고 원본을 다시
읽지 않습니다. 9/16에 보라색으로 바꾸고 서버에도 정상 반영됐는데 카톡에는 계속
9/9의 주황색이 나오고 있었습니다. 이름이 바뀌면 카카오가 모르는 주소라 새로 읽어갑니다.

## 2026-09-17 13:40 KST — `9d0fefe`
DB 변경 기록(`db/CHANGELOG-DB.md`) 신설. 지금까지의 테이블·칼럼 변경 이력을 모아서
정리하고, 각 항목마다 `setup.php` 재실행이 필요한지 표시. 사이트 화면은 바뀐 것 없음
(문서만 추가되어 푸터 버전 표시는 그대로 둠).

## 2026-09-17 12:15 KST — `815eb3e`
헤더·푸터의 보라색 원형 로고(인라인 SVG 스티어링휠 아이콘)를 마스코트 이미지로
교체 — 지금까지 파비콘·og-image만 바꾸고 정작 이 로고는 안 바뀐 채로 남아있었음.

## 2026-09-17 10:51 KST — `e655688`
이미지로 면허 찾기(picker)를 기본 면허 탐색기로 전환. 메인메뉴·푸터의 "면허 탐색기"가
이제 이미지로 고르는 버전으로 연결되고, 텍스트 버전은 보조 화면이 됨. 위저드 첫 화면의
"텍스트로 찾기/이미지로 찾기" 전환 링크를 화면 상단으로 이동.

## 2026-09-16 22:02 KST — `2084ac2`
면허 탐색기 위젯의 JetBrains Mono 글씨 굵기(400) 누락 수정. 사이트 전체 공개 페이지에
"본문 바로가기" 스킵링크 추가(키보드 접근성).

## 2026-09-16 21:56 KST — `a6af7c0`
면허 탐색기 위젯에서 지정만 해두고 실제로 안 불러오던 폰트(Gothic A1, JetBrains Mono)
연결.

## 2026-09-16 21:46 KST — `1b4b4a8`, `25652fe`
Claude Code 스킬 설치: `redesign-existing-projects`(디자인 개선 점검), `web-design-guidelines`
(접근성·UX 점검). React/Tailwind 전제라 이 사이트에 안 맞던 `design-taste-frontend`는
설치 후 제거.

## 2026-09-16 21:37 KST — `2f37e60`
Claude Code 스킬 설치: `web-design-guidelines`.

## 2026-09-16 20:59 KST — `33259d1`
파비콘(브라우저 탭 아이콘)과 카카오톡 공유 미리보기 이미지를 셔틀 마스코트 캐릭터로 교체.

## 2026-09-16 20:55 KST — `855b8c3`
사이트 전체 22개 페이지에 파비콘 연결(이전엔 아예 연결이 안 되어 크롬 탭에 지구본
아이콘이 뜨고 있었음).

---

*(2026-09-17 이전 기록은 git 커밋 로그 참고: `git log --oneline`)*
